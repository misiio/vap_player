# flutter_vap_player example

Demonstrates asset playback in Android texture and platform-view modes, the
iOS platform-view fallback, scale-type changes, repeat and mute controls, and
VAPX dynamic text/image resources.

Run it with `flutter run` from this directory. VAP rendering on iOS must be
verified on a physical device because the upstream renderer does not render on
the simulator.
